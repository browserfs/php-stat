<?php

	namespace foo;

	define('FOO', 'BAR');

	$_GLOBALS['BAR'] = 3;

	function FFo( $bar, $car, \Bar $bamboo = null ) {
		if ( $a === $b ) {
			return true;
		}

		echo "ZBar", "\n";
	}

	interface IBasic extends \IBoo, IBar {

		public function bar( $name );
		protected function moo( $arg1, array $mooarg2 );

	}

	class CBar {

	}